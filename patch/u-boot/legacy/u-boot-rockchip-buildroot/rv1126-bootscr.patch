diff --git a/include/configs/rv1126_common.h b/include/configs/rv1126_common.h
index 970b65e18d..724f6d387d 100644
--- a/include/configs/rv1126_common.h
+++ b/include/configs/rv1126_common.h
@@ -104,8 +104,15 @@
 	"boot_fit;"
 #else
 #define RKIMG_BOOTCOMMAND		\
-	"boot_fit;"			\
-	"boot_android ${devtype} ${devnum};"
+	"setenv devtype mmc;"	\
+	"setenv prefix /boot/;"	\
+	"setenv bootargs;"	\
+	"setenv scriptaddr 0x02000000;" \
+	"setenv devnum 0;"	\
+	"if load ${devtype} ${devnum} ${scriptaddr} ${prefix}boot.scr; then source ${scriptaddr}; fi;" \
+	"setenv devnum 1;"	\
+	"if load ${devtype} ${devnum} ${scriptaddr} ${prefix}boot.scr; then source ${scriptaddr}; fi;" \
+	"echo No Armbian boot.scr found on eMMC or SD;"
 #endif
 #endif
 

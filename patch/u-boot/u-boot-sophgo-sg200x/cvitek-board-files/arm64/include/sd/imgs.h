/* Armbian: unmodified SDK output. The SD build never runs cvi_update - only
 * the -emmc bootloader does - so these vendor names are dead weight here,
 * kept as generated rather than made to look like an Armbian contract. The
 * eMMC variant of this file is the hand-written one.
 */
char imgs[][255] = {"boot.sd",
"logo.jpg",
"rootfs.sd",
};
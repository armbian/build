diff --git a/arch/arm/mach-rockchip/boot_mode.c b/arch/arm/mach-rockchip/boot_mode.c
index 55e9456668..db737e6ca6 100644
--- a/arch/arm/mach-rockchip/boot_mode.c
+++ b/arch/arm/mach-rockchip/boot_mode.c
@@ -34,8 +34,9 @@ void set_back_to_bootrom_dnl_flag(void)
  * make this a weak function that can be override by
  * some special boards.
  */
-#define KEY_DOWN_MIN_VAL	0
-#define KEY_DOWN_MAX_VAL	30
+#define KEY_DOWN_ADC_CHAN	2
+#define KEY_DOWN_MIN_VAL	0
+#define KEY_DOWN_MAX_VAL	83
 
 __weak int rockchip_dnl_key_pressed(void)
 {
@@ -54,7 +55,7 @@ __weak int rockchip_dnl_key_pressed(void)
 
 	ret = -ENODEV;
 	uclass_foreach_dev(dev, uc) {
-		if (!strncmp(dev->name, "saradc", 6)) {
-			ret = adc_channel_single_shot(dev->name, 1, &val);
+		if (!strncmp(dev->name, "saradc", 6)) {
+			ret = adc_channel_single_shot(dev->name, KEY_DOWN_ADC_CHAN, &val);
 			break;
 		}
 	}
-- 
2.49.0

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Juan Sanchez Fuentes <juanesf91@gmail.com>
Date: Thu, 16 Jul 2026 20:15:00 -0400
Subject: [PATCH] mmc: sunxi: fix A523/T527 MMC clock and signal integrity

The Allwinner A523/T527 SoC has several issues in the MMC clock path
and signal integrity that are not present on older sunxi SoCs:

1. The MMC mod clock has a fixed post-divider of 2 that is not
   accounted for in the OSC24M (identification) path.  When running
   at <=24MHz the pll_hz stays at 24MHz, producing wrong divider values.

2. CCLK_DIV=0 is undefined on the A523.  CCLK_DIV must be >= 1,
   which means the card clock is always mod_clock / 2.  Compensate
   by requesting double the mod clock rate.

3. Cap the card clock at 12 MHz to avoid DATA_CRC_ERROR on
   consecutive CMD17 commands due to signal-integrity issues.

4. Force 1-bit bus width on the A523.  4-bit mode produces
   DATA_CRC_ERROR.  1-bit mode uses only DAT0 and is much more
   tolerant.  The BROM itself uses 1-bit mode.

Signed-off-by: Juan Sanchez Fuentes <juanesf91@gmail.com>
---
 drivers/mmc/sunxi_mmc.c | 20 ++++++++++++++++++--
 1 file changed, 18 insertions(+), 2 deletions(-)

diff --git a/drivers/mmc/sunxi_mmc.c b/drivers/mmc/sunxi_mmc.c
index e28c81afffe..e4253990931 100644
--- a/drivers/mmc/sunxi_mmc.c
+++ b/drivers/mmc/sunxi_mmc.c
@@ -77,6 +77,8 @@ static int mmc_set_mod_clk(struct sunxi_mmc_priv *priv, unsigned int hz)
 	if (hz <= 24000000) {
 		pll = CCM_MMC_CTRL_OSCM24;
 		pll_hz = 24000000;
+		if (IS_ENABLED(CONFIG_MACH_SUN55I_A523))
+			pll_hz /= 2;
 	} else {
 #ifdef CONFIG_MACH_SUN9I
 		pll = CCM_MMC_CTRL_PLL_PERIPH0;
@@ -114,6 +116,9 @@ static int mmc_set_mod_clk(struct sunxi_mmc_priv *priv, unsigned int hz)
 		}
 	}

+	if (IS_ENABLED(CONFIG_MACH_SUN55I_A523))
+		hz *= 2;
+
 	div = pll_hz / hz;
 	if (pll_hz % hz)
 		div++;
@@ -204,6 +209,10 @@ static int mmc_update_clk(struct sunxi_mmc_priv *priv)
 static int mmc_config_clock(struct sunxi_mmc_priv *priv, struct mmc *mmc)
 {
 	unsigned rval = readl(&priv->reg->clkcr);
+	unsigned int clock = mmc->clock;
+
+	if (IS_ENABLED(CONFIG_MACH_SUN55I_A523) && clock > 12000000)
+		clock = 12000000;

 	/* Disable Clock */
 	rval &= ~SUNXI_MMC_CLK_ENABLE;
@@ -212,11 +221,13 @@ static int mmc_config_clock(struct sunxi_mmc_priv *priv, struct mmc *mmc)
 		return -1;

 	/* Set mod_clk to new rate */
-	if (mmc_set_mod_clk(priv, mmc->clock))
+	if (mmc_set_mod_clk(priv, clock))
 		return -1;

 	/* Clear internal divider */
 	rval &= ~SUNXI_MMC_CLK_DIVIDER_MASK;
+	if (IS_ENABLED(CONFIG_MACH_SUN55I_A523))
+		rval |= 1;
 	writel(rval, &priv->reg->clkcr);

 #if defined(CONFIG_SUNXI_GEN_SUN6I) || defined(CONFIG_SUN50I_GEN_H6) || defined(CONFIG_SUNXI_GEN_NCAT2)
@@ -573,7 +584,10 @@ struct mmc *sunxi_mmc_init(int sdc_no)
 	cfg->ops  = &sunxi_mmc_ops;

 	cfg->voltages = MMC_VDD_32_33 | MMC_VDD_33_34;
-	cfg->host_caps = MMC_MODE_4BIT;
+	if (IS_ENABLED(CONFIG_MACH_SUN55I_A523))
+		cfg->host_caps = 0;
+	else
+		cfg->host_caps = MMC_MODE_4BIT;

 	if ((IS_ENABLED(CONFIG_MACH_SUN50I) || IS_ENABLED(CONFIG_MACH_SUN8I) ||
 	    IS_ENABLED(CONFIG_SUN50I_GEN_H6) || IS_ENABLED(CONFIG_MACH_SUN55I_A523)) &&
-- 
2.39.5

diff --git a/drivers/mmc/mmc.c b/drivers/mmc/mmc.c
index bf82c515600..900dc6376df 100644
--- a/drivers/mmc/mmc.c
+++ b/drivers/mmc/mmc.c
@@ -357,6 +357,9 @@ int mmc_set_blocklen(struct mmc *mmc, int len)
 	if (mmc->ddr_mode)
 		return 0;

+	if (mmc->high_capacity && len == 512)
+		return 0;
+
 	cmd.cmdidx = MMC_CMD_SET_BLOCKLEN;
 	cmd.resp_type = MMC_RSP_R1;
 	cmd.cmdarg = len;

diff --git a/drivers/media/i2c/techpoint/techpoint_tp2855.c b/drivers/media/i2c/techpoint/techpoint_tp2855.c
index 52275b230..a2deef7ff 100644
--- a/drivers/media/i2c/techpoint/techpoint_tp2855.c
+++ b/drivers/media/i2c/techpoint/techpoint_tp2855.c
@@ -46,9 +46,11 @@ static __maybe_unused const struct regval common_setting_594M_1080p_regs[] = {
 	{ 0x14, 0x33 },
 	{ 0x14, 0xb3 },
 	{ 0x14, 0x33 },
+	{ 0x45, 0x01 },
 
 	// {0x23, 0x02}, //vi test ok
 	// {0x23, 0x00},
+	{0x40, 0x04},		//add this for all channel can be change
 };
 
 static __maybe_unused const struct regval common_setting_594M_720p_1chn_2lane_regs[] = {
@@ -96,39 +98,6 @@ static __maybe_unused const struct regval common_setting_594M_4ch_2lane_720p_25f
 };
 
 static struct techpoint_video_modes supported_modes[] = {
-	{// 4CH 2lane 720p
-		.bus_fmt = MEDIA_BUS_FMT_UYVY8_2X8,
-		.width = 1280,
-		.height = 720,
-		.max_fps = {
-			.numerator = 10000,
-			.denominator = 250000,
-		},
-		.link_freq_value = TP2855_LINK_FREQ_594M,
-		.common_reg_list = common_setting_594M_4ch_2lane_720p_25fps_regs,
-		.common_reg_size = ARRAY_SIZE(common_setting_594M_4ch_2lane_720p_25fps_regs),
-		.bpp = 8,
-		.lane = 2,
-		.vc[PAD0] = 0,
-		.vc[PAD1] = 1,
-		.vc[PAD2] = 2,
-		.vc[PAD3] = 3,
-	},
-	{//1 chn 2 lane 720p
-		.bus_fmt = MEDIA_BUS_FMT_UYVY8_2X8,
-		.width = 1280,
-		.height = 720,
-		.max_fps = {
-			.numerator = 10000,
-			.denominator = 300000,
-			},
-		.link_freq_value = TP2855_LINK_FREQ_594M,
-		.common_reg_list = common_setting_594M_720p_1chn_2lane_regs,
-		.common_reg_size = ARRAY_SIZE(common_setting_594M_720p_1chn_2lane_regs),
-		.bpp = 8,
-		.lane = 2,
-		.vc[PAD0] = 0,
-	},
 	{//4 chn 4 lane 1080p
 		.bus_fmt = MEDIA_BUS_FMT_UYVY8_2X8,
 		.width = 1920,
@@ -183,6 +152,39 @@ static struct techpoint_video_modes supported_modes[] = {
 		.vc[PAD2] = 2,
 		.vc[PAD3] = 3,
 	},
+		{// 4CH 2lane 720p
+		.bus_fmt = MEDIA_BUS_FMT_UYVY8_2X8,
+		.width = 1280,
+		.height = 720,
+		.max_fps = {
+			.numerator = 10000,
+			.denominator = 250000,
+		},
+		.link_freq_value = TP2855_LINK_FREQ_594M,
+		.common_reg_list = common_setting_594M_4ch_2lane_720p_25fps_regs,
+		.common_reg_size = ARRAY_SIZE(common_setting_594M_4ch_2lane_720p_25fps_regs),
+		.bpp = 8,
+		.lane = 2,
+		.vc[PAD0] = 0,
+		.vc[PAD1] = 1,
+		.vc[PAD2] = 2,
+		.vc[PAD3] = 3,
+	},
+	{//1 chn 2 lane 720p
+		.bus_fmt = MEDIA_BUS_FMT_UYVY8_2X8,
+		.width = 1280,
+		.height = 720,
+		.max_fps = {
+			.numerator = 10000,
+			.denominator = 300000,
+			},
+		.link_freq_value = TP2855_LINK_FREQ_594M,
+		.common_reg_list = common_setting_594M_720p_1chn_2lane_regs,
+		.common_reg_size = ARRAY_SIZE(common_setting_594M_720p_1chn_2lane_regs),
+		.bpp = 8,
+		.lane = 2,
+		.vc[PAD0] = 0,
+	},
 };
 
 int tp2855_initialize(struct techpoint *techpoint)
diff --git a/drivers/media/i2c/techpoint/techpoint_v4l2.c b/drivers/media/i2c/techpoint/techpoint_v4l2.c
index 0c441c55f..6611e1822 100644
--- a/drivers/media/i2c/techpoint/techpoint_v4l2.c
+++ b/drivers/media/i2c/techpoint/techpoint_v4l2.c
@@ -496,6 +496,23 @@ static int techpoint_g_frame_interval(struct v4l2_subdev *sd,
 	return 0;
 }
 
+static int techpoint_enum_frame_interval(struct v4l2_subdev *sd,
+				       struct v4l2_subdev_state *sd_state,
+				       struct v4l2_subdev_frame_interval_enum *fie)
+{
+	struct techpoint *techpoint = to_techpoint(sd);
+	
+	if (fie->index >= techpoint->video_modes_num)
+		return -EINVAL;
+
+	fie->code = techpoint->video_modes[0].bus_fmt;
+	fie->width = techpoint->video_modes[fie->index].width;
+	fie->height = techpoint->video_modes[fie->index].height;
+	fie->interval = techpoint->video_modes[fie->index].max_fps;
+
+	return 0;
+}
+
 static int techpoint_g_mbus_config(struct v4l2_subdev *sd,
 				   unsigned int pad_id,
 				   struct v4l2_mbus_config *cfg)
@@ -826,6 +843,7 @@ static const struct v4l2_subdev_pad_ops techpoint_subdev_pad_ops = {
 	.enum_frame_size = techpoint_enum_frame_sizes,
 	.get_fmt = techpoint_get_fmt,
 	.set_fmt = techpoint_set_fmt,
+	.enum_frame_interval = techpoint_enum_frame_interval,
 	.get_mbus_config = techpoint_g_mbus_config,
 };
 

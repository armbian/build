diff --git a/scripts/Makefile.dtbinst b/scripts/Makefile.dtbinst
index 0b674435f63a..a18d02be8866 100644
--- a/scripts/Makefile.dtbinst
+++ b/scripts/Makefile.dtbinst
@@ -22,15 +22,10 @@ quiet_cmd_dtb_install = INSTALL $@
 $(dst)/%: $(obj)/%
 	$(call cmd,dtb_install)
 
-$(dst)/%.scr: $(obj)/%.scr
-	$(call cmd,dtb_install)
-
-$(dst)/README.%-overlays: $(obj)/README.%-overlays
-	$(call cmd,dtb_install)
-
 dtbs := $(patsubst $(obj)/%,%,$(call read-file, $(obj)/dtbs-list))
-script-files	:= $(addprefix $(dst)/, $(scr-y))
-readme-files	:= $(addprefix $(dst)/, $(dtbotxt-y))
+subdirs := $(sort $(dir $(filter %.dtbo, $(dtbs))))
+dtbs += $(patsubst $(obj)/%,%,$(if $(subdirs), $(shell cd $(srctree)/$(obj) && find $(subdirs) -name '*.scr')))
+dtbs += $(patsubst $(obj)/%,%,$(if $(subdirs), $(shell cd $(srctree)/$(obj) && find $(subdirs) -name 'README.*-overlays')))
 
 ifdef CONFIG_ARCH_WANT_FLAT_DTB_INSTALL
 
@@ -41,11 +36,15 @@ endef
 
 $(foreach d, $(sort $(dir $(dtbs))), $(eval $(call gen_install_rules,$(d))))
 
-dtbs := $(notdir $(dtbs))
+# Armbian uses subdirectories such as <allwinner/overlays>.
+#dtbs := $(notdir $(dtbs))
 
 endif # CONFIG_ARCH_WANT_FLAT_DTB_INSTALL
 
-__dtbs_install: $(addprefix $(dst)/, $(dtbs)) $(script-files) $(readme-files)
+__dtbs_install: $(addprefix $(dst)/, $(dtbs))
+	@$(Q)$(kecho) "DT INST: $(dst)"
+	@$(Q)$(kecho) "DT INST: $(dtbs)"
+	@$(Q)$(kecho) "DT INST: $(subdirs)"
 	@:
 
 .PHONY: $(PHONY)

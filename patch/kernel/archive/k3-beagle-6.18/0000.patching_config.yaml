config:

  # Copy BeagleY-AI board supplements and overlays into the TI device-tree
  # source directory after normal kernel patches have been applied.
  dts-directories:
    - { source: "dt", target: "arch/arm64/boot/dts/ti" }

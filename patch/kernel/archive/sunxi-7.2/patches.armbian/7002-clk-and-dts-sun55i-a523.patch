diff --git a/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi b/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi
index 3abbb16..ded3c24 100644
--- a/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi
+++ b/arch/arm64/boot/dts/allwinner/sun55i-a523.dtsi
@@ -3,6 +3,9 @@
 
 #include <dt-bindings/interrupt-controller/arm-gic.h>
 #include <dt-bindings/clock/sun6i-rtc.h>
+#include <dt-bindings/clock/sun8i-de2.h>
+#include <dt-bindings/clock/sun8i-tcon-top.h>
+#include <dt-bindings/reset/sun8i-de2.h>
 #include <dt-bindings/clock/sun55i-a523-ccu.h>
 #include <dt-bindings/thermal/thermal.h>
 #include <dt-bindings/clock/sun55i-a523-mcu-ccu.h>
@@ -19,6 +22,12 @@ / {
 	#address-cells = <2>;
 	#size-cells = <2>;
 
+	de: display-engine {
+		compatible = "allwinner,sun50i-h6-display-engine";
+		allwinner,pipelines = <&mixer0>;
+		status = "disabled";
+	};
+
 	cpus {
 		#address-cells = <1>;
 		#size-cells = <0>;
@@ -147,6 +156,220 @@ gpu: gpu@1800000 {
 			status = "disabled";
 		};
 
+		/*
+		 * Display pipeline (DE33 -> TCON-TOP -> TCON-TV -> DW-HDMI ->
+		 * Inno HDMI PHY). Addresses from the vendor DTB; HDMI PHY split
+		 * (controller@5520000 + phy@5530000) and the DE SRAM region are
+		 * best-guesses pending hardware verification. Most nodes reuse
+		 * the H6/H616 drivers via compatible fallbacks; only CLK_HDMI
+		 * (TMDS) and the hdmi-phy variant were added for the A523.
+		 */
+		bus@5000000 {
+			compatible = "allwinner,sun50i-h616-de33",
+				     "allwinner,sun50i-a64-de2";
+			reg = <0x5000000 0x400000>;
+			allwinner,sram = <&de_sram 1>;
+			#address-cells = <1>;
+			#size-cells = <1>;
+			ranges = <0 0x5000000 0x400000>;
+
+			display_clocks: clock@8000 {
+				compatible = "allwinner,sun50i-h616-de33-clk";
+				reg = <0x8000 0x100>;
+				clocks = <&ccu CLK_DE>, <&ccu CLK_BUS_DE>;
+				clock-names = "mod", "bus";
+				resets = <&ccu RST_BUS_DE>;
+				power-domains = <&pck600 PD_DE>;
+				#clock-cells = <1>;
+				#reset-cells = <1>;
+			};
+
+			mixer0: mixer@100000 {
+				compatible = "allwinner,sun55i-a523-de33-mixer-0";
+				reg = <0x100000 0x100000>,
+				      <0x8100 0x40>,
+				      <0x280000 0x20000>,
+				      <0x8000 0x60>;
+				reg-names = "engine", "top", "display", "detop";
+				clocks = <&display_clocks CLK_BUS_MIXER0>,
+					 <&display_clocks CLK_MIXER0>;
+				clock-names = "bus", "mod";
+				resets = <&display_clocks RST_MIXER0>;
+				power-domains = <&pck600 PD_DE>;
+
+				ports {
+					#address-cells = <1>;
+					#size-cells = <0>;
+
+					mixer0_out: port@1 {
+						reg = <1>;
+
+						mixer0_out_tcon_top_mixer0: endpoint {
+							remote-endpoint = <&tcon_top_mixer0_in_mixer0>;
+						};
+					};
+				};
+			};
+		};
+
+		tcon_top: tcon-top@5500000 {
+			compatible = "allwinner,sun50i-h6-tcon-top";
+			reg = <0x5500000 0x1000>;
+			clocks = <&ccu CLK_BUS_DISPLAY0_TOP>,
+				 <&ccu CLK_TCON_TV0>;
+			clock-names = "bus", "tcon-tv0";
+			clock-output-names = "tcon-top-tv0";
+			resets = <&ccu RST_BUS_DISPLAY0_TOP>;
+			#clock-cells = <1>;
+
+			ports {
+				#address-cells = <1>;
+				#size-cells = <0>;
+
+				tcon_top_mixer0_in: port@0 {
+					#address-cells = <1>;
+					#size-cells = <0>;
+					reg = <0>;
+
+					tcon_top_mixer0_in_mixer0: endpoint@0 {
+						reg = <0>;
+						remote-endpoint = <&mixer0_out_tcon_top_mixer0>;
+					};
+				};
+
+				tcon_top_mixer0_out: port@1 {
+					#address-cells = <1>;
+					#size-cells = <0>;
+					reg = <1>;
+
+					tcon_top_mixer0_out_tcon_tv0: endpoint@2 {
+						reg = <2>;
+						remote-endpoint = <&tcon_tv0_in_tcon_top_mixer0>;
+					};
+				};
+
+				tcon_top_hdmi_in: port@4 {
+					#address-cells = <1>;
+					#size-cells = <0>;
+					reg = <4>;
+
+					tcon_top_hdmi_in_tcon_tv0: endpoint@0 {
+						reg = <0>;
+						remote-endpoint = <&tcon_tv0_out_tcon_top>;
+					};
+				};
+
+				tcon_top_hdmi_out: port@5 {
+					reg = <5>;
+
+					tcon_top_hdmi_out_hdmi: endpoint {
+						remote-endpoint = <&hdmi_in_tcon_top>;
+					};
+				};
+			};
+		};
+
+		tcon_tv0: lcd-controller@5503000 {
+			compatible = "allwinner,sun50i-h6-tcon-tv",
+				     "allwinner,sun8i-r40-tcon-tv";
+			reg = <0x5503000 0x1000>;
+			interrupts = <GIC_SPI 91 IRQ_TYPE_LEVEL_HIGH>;
+			clocks = <&ccu CLK_BUS_TCON_TV0>,
+				 <&tcon_top CLK_TCON_TOP_TV0>;
+			clock-names = "ahb", "tcon-ch1";
+			resets = <&ccu RST_BUS_TCON_TV0>;
+			reset-names = "lcd";
+
+			ports {
+				#address-cells = <1>;
+				#size-cells = <0>;
+
+				tcon_tv0_in: port@0 {
+					reg = <0>;
+
+					tcon_tv0_in_tcon_top_mixer0: endpoint {
+						remote-endpoint = <&tcon_top_mixer0_out_tcon_tv0>;
+					};
+				};
+
+				tcon_tv0_out: port@1 {
+					#address-cells = <1>;
+					#size-cells = <0>;
+					reg = <1>;
+
+					tcon_tv0_out_tcon_top: endpoint@1 {
+						reg = <1>;
+						remote-endpoint = <&tcon_top_hdmi_in_tcon_tv0>;
+					};
+				};
+			};
+		};
+
+		hdmi: hdmi@5520000 {
+			compatible = "allwinner,sun55i-a523-dw-hdmi",
+				     "allwinner,sun50i-h6-dw-hdmi";
+			reg = <0x5520000 0x10000>;
+			reg-io-width = <1>;
+			interrupts = <GIC_SPI 93 IRQ_TYPE_LEVEL_HIGH>;
+			clocks = <&ccu CLK_BUS_HDMI>, <&ccu CLK_HDMI_24M>,
+				 <&ccu CLK_HDMI>, <&ccu CLK_HDMI_CEC>;
+			clock-names = "iahb", "isfr", "tmds", "cec";
+			resets = <&ccu RST_BUS_HDMI_MAIN>;
+			reset-names = "ctrl";
+			power-domains = <&pck600 PD_VO0>;
+			phys = <&hdmi_phy>;
+			phy-names = "phy";
+			status = "disabled";
+
+			ports {
+				#address-cells = <1>;
+				#size-cells = <0>;
+
+				hdmi_in: port@0 {
+					reg = <0>;
+
+					hdmi_in_tcon_top: endpoint {
+						remote-endpoint = <&tcon_top_hdmi_out_hdmi>;
+					};
+				};
+
+				hdmi_out: port@1 {
+					reg = <1>;
+				};
+			};
+		};
+
+		hdmi_phy: hdmi-phy@5530000 {
+			compatible = "allwinner,sun55i-a523-hdmi-phy",
+				     "allwinner,sun50i-h616-hdmi-phy";
+			reg = <0x5530000 0x10000>;
+			clocks = <&ccu CLK_BUS_HDMI>, <&ccu CLK_HDMI_24M>;
+			clock-names = "bus", "mod";
+			resets = <&ccu RST_BUS_HDMI_SUB>;
+			reset-names = "phy";
+			power-domains = <&pck600 PD_VO0>;
+			#phy-cells = <0>;
+		};
+
+		video-codec@1c0e000 {
+			compatible = "allwinner,sun55i-a523-video-engine";
+			reg = <0x01c0e000 0x2000>;
+			interrupts = <GIC_SPI 120 IRQ_TYPE_LEVEL_HIGH>;
+			clocks = <&ccu CLK_BUS_VE>, <&ccu CLK_VE>,
+				 <&ccu CLK_MBUS_VE>;
+			clock-names = "ahb", "mod", "ram";
+			resets = <&ccu RST_BUS_VE>;
+/*
+			 * Minimal bring-up: no iommus/allwinner,sram. The driver
+			 * marks this variant quirk_no_sram and skips the SRAM
+			 * claim, and the IOMMU is in bypass at reset so the VE
+			 * does physical DMA from the CMA pool. Add
+			 *   iommus = <&iommu 3>;
+			 *   allwinner,sram = <&ve_sram 1>;
+			 * once an IOMMU node + ve_sram region are wired up.
+			 */
+		};
+
 		pio: pinctrl@2000000 {
 			compatible = "allwinner,sun55i-a523-pinctrl";
 			reg = <0x2000000 0x800>;
@@ -604,6 +827,13 @@ syscon: syscon@3000000 {
 			#address-cells = <1>;
 			#size-cells = <1>;
 			ranges;
+
+			/* DE (display engine) SRAM region claimed by the DE33 bus */
+			de_sram: sram-section@0 {
+				compatible = "allwinner,sun50i-h616-sram-c",
+					     "allwinner,sun50i-a64-sram-c";
+				reg = <0x0000 0x1e000>;
+			};
 		};
 
 		dma: dma-controller@3002000 {
diff --git a/arch/arm64/boot/dts/allwinner/sun55i-a527-cubie-a5e.dts b/arch/arm64/boot/dts/allwinner/sun55i-a527-cubie-a5e.dts
index f9a79ba..c036a4b 100644
--- a/arch/arm64/boot/dts/allwinner/sun55i-a527-cubie-a5e.dts
+++ b/arch/arm64/boot/dts/allwinner/sun55i-a527-cubie-a5e.dts
@@ -1,5 +1,6 @@
 // SPDX-License-Identifier: (GPL-2.0-only OR MIT)
 // Copyright (C) 2025 Arm Ltd.
+// Author: piotr.oniszczuk@gmail.com
 
 /dts-v1/;
 
@@ -13,6 +14,9 @@
 	compatible = "radxa,cubie-a5e", "allwinner,sun55i-a527";
 
 	aliases {
+		mmc0 = &mmc0;
+		mmc1 = &mmc1;
+		mmc2 = &mmc2;
 		ethernet0 = &gmac0;
 		ethernet1 = &gmac1;
 		serial0 = &uart0;
@@ -23,6 +27,75 @@
 		stdout-path = "serial0:115200n8";
 	};
 
+	reserved-memory {
+		#address-cells = <2>;
+		#size-cells = <2>;
+		ranges;
+
+		/*
+		 * The display engine's layer DMA master has a limited fetch
+		 * aperture and can only reach low DRAM (it reads the RCQ pool at
+		 * ~0x43000000 but not 0x4b100000). The kernel's default CMA is
+		 * high (~0xf0000000), so scanout fetches zeros (black). Pin CMA
+		 * just above the kernel image (ends ~0x42c70000) and the RCQ
+		 * pool (~0x43000000), below the dtb (~0x49ff6000).
+		 */
+		riscvsram0_reserved: riscvsram0@7280000 {
+			reg = <0x0 0x07280000 0x0 0x40000>;
+			no-map;
+		};
+
+		riscvsram1_reserved: riscvsram1@72c0000 {
+			reg = <0x0 0x072c0000 0x0 0x40000>;
+			no-map;
+		};
+
+		e906_dram_reserved: e906_dram@60000000 {
+			reg = <0x0 0x60000000 0x0 0xa00000>;
+			no-map;
+		};
+
+		rv_vdev0buffer: vdev0buffer@4ae00000 {
+			compatible = "shared-dma-pool";
+			reg = <0x0 0x4ae00000 0x0 0x40000>;
+			no-map;
+		};
+
+		rv_vdev0vring0: vdev0vring0@4ae40000 {
+			reg = <0x0 0x4ae40000 0x0 0x2000>;
+			no-map;
+		};
+
+		rv_vdev0vring1: vdev0vring1@4ae42000 {
+			reg = <0x0 0x4ae42000 0x0 0x2000>;
+			no-map;
+		};
+
+		linux,cma {
+			compatible = "shared-dma-pool";
+			reusable;
+			/* pin low: the DE fetch aperture only reaches DRAM below
+			 * ~0x4b100000; stop at 0x48000000 to stay clear of the
+			 * secmon@48000000/48100000 and vdev@4ae00000 no-map
+			 * reservations (overlap hands out no-map pages -> oops) */
+			reg = <0x0 0x43000000 0x0 0x5000000>;
+			alloc-ranges = <0x0 0x43000000 0x0 0x5000000>;
+			size = <0x0 0x5000000>;
+			linux,cma-default;
+		};
+	};
+
+	hdmi-connector {
+		compatible = "hdmi-connector";
+		type = "a";
+
+		port {
+			hdmi_con_in: endpoint {
+				remote-endpoint = <&hdmi_out_con>;
+			};
+		};
+	};
+
 	leds {
 		compatible = "gpio-leds";
 
@@ -38,6 +111,7 @@
 			function = LED_FUNCTION_ACTIVITY;
 			color = <LED_COLOR_ID_BLUE>;
 			gpios = <&r_pio 0 5 GPIO_ACTIVE_LOW>; /* PL5 */
+			linux,default-trigger = "disk-activity";
 		};
 	};
 
@@ -113,6 +187,15 @@
 		gpio = <&pio 1 6 GPIO_ACTIVE_HIGH>;
 		enable-active-high;
 	};
+
+};
+
+&cpu0 {
+	cpu-supply = <&reg_dcdc1>;
+};
+
+&cpu4 {
+	cpu-supply = <&reg_dcdc1_323>;
 };
 
 &ehci0 {
@@ -275,6 +358,7 @@
 			};
 
 			reg_aldo2: aldo2 {
+				regulator-always-on;
 				regulator-min-microvolt = <1800000>;
 				regulator-max-microvolt = <1800000>;
 				regulator-name = "vcc-pe";
@@ -310,6 +394,7 @@
 			};
 
 			reg_bldo3: bldo3 {
+				regulator-always-on;
 				regulator-min-microvolt = <3300000>;
 				regulator-max-microvolt = <3300000>;
 				regulator-name = "vcc-mipi-cam";
@@ -320,6 +405,7 @@
 			};
 
 			reg_cldo1: cldo1 {
+				regulator-always-on;
 				regulator-min-microvolt = <1800000>;
 				regulator-max-microvolt = <1800000>;
 				regulator-name = "vcc-pc-and-their-dog";
@@ -489,3 +575,41 @@
 	status = "okay";
 };
 
+
+&e906_rproc {
+	mboxes = <&msgbox 8>, <&msgbox 10>;
+	mbox-names = "arm-kick", "arm-standby";
+	memory-region = <&riscvsram0_reserved>, <&riscvsram1_reserved>,
+			<&rv_vdev0buffer>, <&rv_vdev0vring0>,
+			<&rv_vdev0vring1>, <&e906_dram_reserved>;
+	memory-mappings =
+		/* < DA		len		PA >	*/
+		/* DSP RAM */
+		< 0x20000	0x20000		0x20000 >,
+		/* SRAM A2 */
+		< 0x40000	0x24000		0x40000 >,
+		/* DDR */
+		< 0x8000000	0x37f00000	0x8000000 >,
+		/* SRAM SPACE 0 */
+		< 0x3ffc0000	0x40000		0x07280000 >,
+		/* SRAM SPACE 1 */
+		< 0x40000000	0x40000		0x072c0000 >,
+		/* DRAM SPACE */
+		< 0x40040000	0x3ffc0000	0x40040000 >;
+	status = "okay";
+};
+
+&de {
+	status = "okay";
+};
+
+&hdmi {
+	hvcc-supply = <&reg_cldo1>;
+	status = "okay";
+};
+
+&hdmi_out {
+	hdmi_out_con: endpoint {
+		remote-endpoint = <&hdmi_con_in>;
+	};
+};
diff --git a/arch/arm64/boot/dts/allwinner/sun55i-t527-avaota-a1.dts b/arch/arm64/boot/dts/allwinner/sun55i-t527-avaota-a1.dts
index 3bcd11e..2d90356 100644
--- a/arch/arm64/boot/dts/allwinner/sun55i-t527-avaota-a1.dts
+++ b/arch/arm64/boot/dts/allwinner/sun55i-t527-avaota-a1.dts
@@ -59,6 +59,19 @@ rv_vdev0vring1: vdev0vring1@4ae42000 {
 			reg = <0x0 0x4ae42000 0x0 0x2000>;
 			no-map;
 		};
+
+		linux,cma {
+			compatible = "shared-dma-pool";
+			reusable;
+			/* pin low: the DE fetch aperture only reaches DRAM below
+			 * ~0x4b100000; stop at 0x48000000 to stay clear of the
+			 * secmon@48000000/48100000 and vdev@4ae00000 no-map
+			 * reservations (overlap hands out no-map pages -> oops) */
+			reg = <0x0 0x43000000 0x0 0x5000000>;
+			alloc-ranges = <0x0 0x43000000 0x0 0x5000000>;
+			size = <0x0 0x5000000>;
+			linux,cma-default;
+		};
 	};
 
 	ext_osc32k: ext-osc32k-clk {
@@ -108,6 +121,17 @@ wifi_pwrseq: wifi-pwrseq {
 		reset-gpios = <&pio 6 13 GPIO_ACTIVE_LOW>;	/* PG13 WL-REG-ON */
 		post-power-on-delay-ms = <200>;
 	};
+
+	hdmi-connector {
+		compatible = "hdmi-connector";
+		type = "a";
+
+		port {
+			hdmi_con_in: endpoint {
+				remote-endpoint = <&hdmi_out_con>;
+			};
+		};
+	};
 };
 
 &ehci0 {
@@ -584,3 +608,18 @@ &e906_rproc {
 		< 0x40040000	0x3ffc0000	0x40040000 >;
 	status = "okay";
 };
+
+&de {
+	status = "okay";
+};
+
+&hdmi {
+	hvcc-supply = <&reg_bldo3>;
+	status = "okay";
+};
+
+&hdmi_out {
+	hdmi_out_con: endpoint {
+		remote-endpoint = <&hdmi_con_in>;
+	};
+};
diff --git a/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts b/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts
index e2eefc3..ef9dead 100644
--- a/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts
+++ b/arch/arm64/boot/dts/allwinner/sun55i-t527-orangepi-4a.dts
@@ -23,6 +23,53 @@ chosen {
 		stdout-path = "serial0:115200n8";
 	};
 
+	reserved-memory {
+		#address-cells = <2>;
+		#size-cells = <2>;
+		ranges;
+
+		/* 128 KiB reserved for Trusted Firmware-A (BL31). */
+		secmon@48000000 {
+			reg = <0x0 0x48000000 0x0 0x20000>;
+			no-map;
+		};
+
+		/* 256 KiB reserved for the SCP. */
+		secmon@48100000 {
+			reg = <0x0 0x48100000 0x0 0x40000>;
+			no-map;
+		};
+
+		/*
+		 * The display engine's layer DMA master has a limited fetch
+		 * aperture and can only reach low DRAM (it reads the RCQ pool at
+		 * ~0x43000000 but not 0x4b100000). The kernel's default CMA is
+		 * high (~0xf0000000), so scanout fetches zeros (black). Ideally
+		 * this pool should be pinned low just above the kernel image
+		 * (ends ~0x42c70000) and the RCQ pool (~0x43000000), below the
+		 * dtb (~0x49ff6000); that is NOT guaranteed without reg and
+		 * alloc-ranges. HDMI output on this board is untested.
+		 */
+		linux,cma {
+			compatible = "shared-dma-pool";
+			reusable;
+			size = <0x0 0x10000000>;
+			linux,cma-default;
+		};
+	};
+
+	/* HDMI output is enabled but not yet validated on this board. */
+	hdmi-connector {
+		compatible = "hdmi-connector";
+		type = "a";
+
+		port {
+			hdmi_con_in: endpoint {
+				remote-endpoint = <&hdmi_out_con>;
+			};
+		};
+	};
+
 	ext_osc32k: ext-osc32k-clk {
 		#clock-cells = <0>;
 		compatible = "fixed-clock";
@@ -472,3 +519,17 @@ &usbphy {
 	usb1_vbus-supply = <&reg_usb_vbus>;
 	status = "okay";
 };
+
+&de {
+	status = "okay";
+};
+
+&hdmi {
+	status = "okay";
+};
+
+&hdmi_out {
+	hdmi_out_con: endpoint {
+		remote-endpoint = <&hdmi_con_in>;
+	};
+};
diff --git a/drivers/clk/sunxi-ng/ccu-sun55i-a523.c b/drivers/clk/sunxi-ng/ccu-sun55i-a523.c
index d469a6b..e52bfec 100644
--- a/drivers/clk/sunxi-ng/ccu-sun55i-a523.c
+++ b/drivers/clk/sunxi-ng/ccu-sun55i-a523.c
@@ -120,7 +120,7 @@ static const struct clk_hw *pll_periph0_150M_hws[] = {
 
 #define SUN55I_A523_PLL_PERIPH1_REG	0x028
 static struct ccu_nm pll_periph1_4x_clk = {
-	.enable		= BIT(27),
+	.enable		= BIT(27) | BIT(30) | BIT(31),
 	.lock		= BIT(28),
 	.n		= _SUNXI_CCU_MULT_MIN(8, 8, 11),
 	.m		= _SUNXI_CCU_DIV(1, 1), /* input divider */
@@ -128,7 +128,8 @@ static struct ccu_nm pll_periph1_4x_clk = {
 		.reg		= 0x028,
 		.hw.init	= CLK_HW_INIT_PARENTS_DATA("pll-periph1-4x",
 							   osc24M, &ccu_nm_ops,
-							   CLK_SET_RATE_GATE),
+							   CLK_SET_RATE_UNGATE |
+							   CLK_IS_CRITICAL),
 	},
 };
 
@@ -683,10 +684,10 @@ static SUNXI_CCU_MP_MUX_GATE_POSTDIV_DUALDIV(mmc1_clk, "mmc1", nand_mmc_parents,
 
 static const struct clk_parent_data mmc2_parents[] = {
 	{ .fw_name = "hosc" },
+	{ .hw = &pll_periph1_600M_clk.hw },
 	{ .hw = &pll_periph0_800M_clk.common.hw },
 	{ .hw = &pll_periph0_600M_clk.hw },
 	{ .hw = &pll_periph1_800M_clk.common.hw },
-	{ .hw = &pll_periph1_600M_clk.hw },
 };
 
 static SUNXI_CCU_MP_MUX_GATE_POSTDIV_DUALDIV(mmc2_clk, "mmc2", mmc2_parents,
@@ -904,6 +905,23 @@ static SUNXI_CCU_GATE_HWS(bus_display1_top_clk, "bus-display1-top", ahb_hws,
 
 static SUNXI_CCU_GATE_DATA(hdmi_24M_clk, "hdmi-24M", osc24M, 0xb04, BIT(31), 0);
 
+/*
+ * Main HDMI TMDS ("mod") clock. Register 0xb00 follows the H616 layout
+ * (hdmi-24M@0xb04 and bus-hdmi@0xb1c are byte-identical to H616), sourced
+ * from the video PLLs; the dw-hdmi driver sets it to the pixel clock.
+ */
+static const struct clk_hw *hdmi_tmds_parents[] = {
+	&pll_video0_4x_clk.common.hw,
+	&pll_video1_4x_clk.common.hw,
+	&pll_video2_4x_clk.common.hw,
+	&pll_video3_4x_clk.common.hw,
+};
+static SUNXI_CCU_M_HW_WITH_MUX_GATE(hdmi_clk, "hdmi", hdmi_tmds_parents, 0xb00,
+				    0, 4,	/* M */
+				    24, 2,	/* mux */
+				    BIT(31),	/* gate */
+				    CLK_SET_RATE_PARENT);
+
 static SUNXI_CCU_GATE_HWS_WITH_PREDIV(hdmi_cec_32k_clk, "hdmi-cec-32k",
 				      pll_periph0_2x_hws,
 				      0xb10, BIT(30), 36621, 0);
@@ -1329,6 +1347,7 @@ static struct ccu_common *sun55i_a523_ccu_clks[] = {
 	&bus_display0_top_clk.common,
 	&bus_display1_top_clk.common,
 	&hdmi_24M_clk.common,
+	&hdmi_clk.common,
 	&hdmi_cec_32k_clk.common,
 	&hdmi_cec_clk.common,
 	&bus_hdmi_clk.common,
@@ -1513,6 +1532,7 @@ static struct clk_hw_onecell_data sun55i_a523_hw_clks = {
 		[CLK_BUS_DISPLAY0_TOP]	= &bus_display0_top_clk.common.hw,
 		[CLK_BUS_DISPLAY1_TOP]	= &bus_display1_top_clk.common.hw,
 		[CLK_HDMI_24M]		= &hdmi_24M_clk.common.hw,
+		[CLK_HDMI]		= &hdmi_clk.common.hw,
 		[CLK_HDMI_CEC_32K]	= &hdmi_cec_32k_clk.common.hw,
 		[CLK_HDMI_CEC]		= &hdmi_cec_clk.common.hw,
 		[CLK_BUS_HDMI]		= &bus_hdmi_clk.common.hw,
@@ -1556,7 +1576,7 @@ static struct clk_hw_onecell_data sun55i_a523_hw_clks = {
 		[CLK_NPU]		= &npu_clk.common.hw,
 		[CLK_USB3_REF]		= &usb3_ref_clk.common.hw,
 	},
-	.num	= CLK_USB3_REF + 1,
+	.num	= CLK_HDMI + 1,
 };
 
 static struct ccu_reset_map sun55i_a523_ccu_resets[] = {
diff --git a/include/dt-bindings/clock/sun55i-a523-ccu.h b/include/dt-bindings/clock/sun55i-a523-ccu.h
index 02cc8c4..2eca83f 100644
--- a/include/dt-bindings/clock/sun55i-a523-ccu.h
+++ b/include/dt-bindings/clock/sun55i-a523-ccu.h
@@ -187,5 +187,6 @@
 #define CLK_FANOUT2		178
 #define CLK_NPU			179
 #define CLK_USB3_REF		180
+#define CLK_HDMI		181
 
 #endif /* _DT_BINDINGS_CLK_SUN55I_A523_CCU_H_ */
diff --git a/include/dt-bindings/reset/sun55i-a523-ccu.h b/include/dt-bindings/reset/sun55i-a523-ccu.h
index 70df503..4438d15 100644
--- a/include/dt-bindings/reset/sun55i-a523-ccu.h
+++ b/include/dt-bindings/reset/sun55i-a523-ccu.h
@@ -84,5 +84,6 @@
 #define RST_BUS_LEDC		75
 #define RST_BUS_CSI		76
 #define RST_BUS_ISP		77
+#define RST_BUS_IOMMU		78
 
 #endif /* _DT_BINDINGS_RST_SUN55I_A523_CCU_H_ */

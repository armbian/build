From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Jason Perlow <jperlow@gmail.com>
Date: Thu, 2 Jul 2026 22:12:37 -0400
Subject: ASoC: cix: fix 6.6->7.2 API drift (rtd->id, daifmt parse, dpcm flags,
 SOF_IPC_TYPE_3, NS string literals, void remove)

---
 drivers/gpu/drm/cix/cix_display.c                                      |  4 +-
 drivers/gpu/drm/cix/cix_virtual.c                                      |  9 +--
 drivers/gpu/drm/cix/dptx/cix_edp_panel.c                               |  2 +-
 drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c                             |  3 +-
 drivers/gpu/drm/cix/dptx/trilin_drm.c                                  | 26 +++----
 drivers/gpu/drm/cix/linlon-dp/hw/dp_component.c                        | 14 ++--
 drivers/gpu/drm/cix/linlon-dp/linlondp_cluster.c                       |  3 +-
 drivers/gpu/drm/cix/linlon-dp/linlondp_crtc.c                          |  2 +-
 drivers/gpu/drm/cix/linlon-dp/linlondp_drv.c                           |  9 +--
 drivers/gpu/drm/cix/linlon-dp/linlondp_pipeline_state.c                |  2 +-
 drivers/soc/cix/cix_ap2se_ipc.c                                        |  3 +-
 drivers/soc/cix/cix_dst/blackbox/exception_trace/rdr_exception_trace.c |  3 +-
 drivers/soc/cix/cix_dst/blackbox/platform_ap/rdr_ap_adapter.c          |  3 +-
 drivers/soc/cix/cix_dst/blackbox/rdr_core.c                            |  3 +-
 drivers/soc/cix/cix_dst/se_crash_monitor/se_crash_drv.c                |  3 +-
 sound/soc/cix/card-utils.c                                             | 37 ++++------
 sound/soc/cix/cdns_i2s_mc.c                                            |  3 +-
 sound/soc/sof/cix/cix-sky1.c                                           | 15 ++--
 18 files changed, 62 insertions(+), 82 deletions(-)

diff --git a/drivers/gpu/drm/cix/cix_display.c b/drivers/gpu/drm/cix/cix_display.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/cix_display.c
+++ b/drivers/gpu/drm/cix/cix_display.c
@@ -177,13 +177,13 @@ static int __init cix_acpi_display_probe(void)
        return 0;
 }
 
-static int cix_display_remove(struct platform_device *pdev)
+static void cix_display_remove(struct platform_device *pdev)
 {
        struct device *dev = &pdev->dev;
 
        dev_set_drvdata(dev, NULL);
 
-       return 0;
+       return;
 }
 
 struct platform_driver cix_display_driver = {
diff --git a/drivers/gpu/drm/cix/cix_virtual.c b/drivers/gpu/drm/cix/cix_virtual.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/cix_virtual.c
+++ b/drivers/gpu/drm/cix/cix_virtual.c
@@ -234,9 +234,9 @@ cix_virtual_bind(struct device *comp, struct device *master,
 	}
 
 	/*  Set supported color formats */
-	connector->display_info.color_formats = DRM_COLOR_FORMAT_RGB444   |
-						DRM_COLOR_FORMAT_YCBCR422 |
-						DRM_COLOR_FORMAT_YCBCR420;
+	connector->display_info.color_formats = BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444)   |
+						BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR422) |
+						BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420);
 
 	return 0;
 }
@@ -284,7 +284,7 @@ static int cix_virtual_probe(struct platform_device *pdev)
 	return component_add(dev, &cix_virtual_ops);
 }
 
-static int cix_virtual_remove(struct platform_device *pdev)
+static void cix_virtual_remove(struct platform_device *pdev)
 {
 	struct device *dev = &pdev->dev;
 
@@ -292,7 +292,6 @@ static int cix_virtual_remove(struct platform_device *pdev)
 
 	dev_set_drvdata(dev, NULL);
 
-	return 0;
 }
 
 struct platform_driver cix_virtual_driver = {
diff --git a/drivers/gpu/drm/cix/dptx/cix_edp_panel.c b/drivers/gpu/drm/cix/dptx/cix_edp_panel.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/dptx/cix_edp_panel.c
+++ b/drivers/gpu/drm/cix/dptx/cix_edp_panel.c
@@ -843,7 +843,7 @@ static int cix_edp_panel_platform_probe(struct platform_device *pdev)
 	return cix_edp_panel_probe(&pdev->dev, desc);
 }
 
-static int cix_edp_panel_platform_remove(struct platform_device *pdev)
+static void cix_edp_panel_platform_remove(struct platform_device *pdev)
 {
 	return cix_edp_panel_remove(&pdev->dev);
 }
diff --git a/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c b/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c
+++ b/drivers/gpu/drm/cix/dptx/trilin_dptx_cix.c
@@ -215,10 +215,9 @@ static int trilin_dptx_cix_probe(struct platform_device *pdev)
 	return component_add(&pdev->dev, &trilin_dptx_cix_ops);
 }
 
-static int trilin_dptx_cix_remove(struct platform_device *pdev)
+static void trilin_dptx_cix_remove(struct platform_device *pdev)
 {
 	component_del(&pdev->dev, &trilin_dptx_cix_ops);
-	return 0;
 }
 
 #ifdef CONFIG_PM
diff --git a/drivers/gpu/drm/cix/dptx/trilin_drm.c b/drivers/gpu/drm/cix/dptx/trilin_drm.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/dptx/trilin_drm.c
+++ b/drivers/gpu/drm/cix/dptx/trilin_drm.c
@@ -810,14 +810,14 @@ static u8 trilin_dp_cal_bpp(u8 bpc, int format)
 	u8 bpp = bpc * 3;
 
 	switch (format) {
-	case DRM_COLOR_FORMAT_RGB444:
-	case DRM_COLOR_FORMAT_YCBCR444:
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444):
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR444):
 		bpp = bpc * 3;
 		break;
-	case DRM_COLOR_FORMAT_YCBCR422:
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR422):
 		bpp = bpc * 2;
 		break;
-	case DRM_COLOR_FORMAT_YCBCR420:
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420):
 		bpp = bpc * 3 / 2;
 		break;
 	default:
@@ -940,7 +940,7 @@ static bool compute_available_clock_rate(struct trilin_dp *dp,
 		struct drm_connector_state *connector_state, u8 suggest_bpc,
 		int clock, int color_format, int *rt_bpc, int *rt_bpp)
 {
-	int min_bpc = (color_format == DRM_COLOR_FORMAT_RGB444) ? 6 : 8;
+	int min_bpc = (color_format == BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444)) ? 6 : 8;
 	int bpc = max(suggest_bpc, min_bpc);
 	int bpp, max_pixel_clock;
 	u8 max_lanes = dp->link_config.max_lanes;
@@ -992,17 +992,17 @@ int trilin_dp_encoder_compute_config(struct drm_encoder *encoder,
 	u8 max_lanes = dp->link_config.max_lanes;
 	int link_rate = max_rate;
 
-	if (!(info_formats & DRM_COLOR_FORMAT_RGB444)) {
-		info_formats |= DRM_COLOR_FORMAT_RGB444;
+	if (!(info_formats & BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444))) {
+		info_formats |= BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444);
 		DP_WARN("info_format=%0x, force support RGB444", info->color_formats);
 	}
 
 	const int COMMON_COLORS_FORMATS[] = {
-		DRM_COLOR_FORMAT_RGB444, DRM_COLOR_FORMAT_YCBCR422, DRM_COLOR_FORMAT_YCBCR420};
+		BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444), BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR422), BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420)};
 
 	if (drm_mode_is_420_only(info, adjusted_mode)) {
 		/* CEA YUV420-only timings must be driven as YCbCr 4:2:0 */
-		color_format = DRM_COLOR_FORMAT_YCBCR420;
+		color_format = BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420);
 		success = compute_available_clock_rate(dp, connector_state, suggest_bpc,
 			adjusted_mode->clock, color_format, &bpc, &bpp);
 		if (success)
@@ -1015,7 +1015,7 @@ int trilin_dp_encoder_compute_config(struct drm_encoder *encoder,
 					suggest_bpc, adjusted_mode->clock, color_format,
 					&bpc, &bpp);
 				if (success) {
-					if (color_format != DRM_COLOR_FORMAT_RGB444)
+					if (color_format != BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444))
 						DP_INFO("Use YUV Format=0x%0x", color_format);
 					break;
 				}
@@ -1030,13 +1030,13 @@ int trilin_dp_encoder_compute_config(struct drm_encoder *encoder,
 	}
 
 	switch (color_format) {
-	case DRM_COLOR_FORMAT_YCBCR444:
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR444):
 		format = TRILIN_DPSUB_FORMAT_YCBCR444;
 		break;
-	case DRM_COLOR_FORMAT_YCBCR422:
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR422):
 		format = TRILIN_DPSUB_FORMAT_YCBCR422;
 		break;
-	case DRM_COLOR_FORMAT_YCBCR420:
+	case BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420):
 		format = TRILIN_DPSUB_FORMAT_YCBCR420;
 		break;
 	default:
diff --git a/drivers/gpu/drm/cix/linlon-dp/hw/dp_component.c b/drivers/gpu/drm/cix/linlon-dp/hw/dp_component.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/linlon-dp/hw/dp_component.c
+++ b/drivers/gpu/drm/cix/linlon-dp/hw/dp_component.c
@@ -1179,11 +1179,11 @@ static void dp_improc_update(struct linlondp_component *c,
 		IPS_CTRL_CHD420 | IPS_CTRL_DITH;
 
 	/* config color format */
-	if (st->color_format == DRM_COLOR_FORMAT_YCBCR420)
+	if (st->color_format == BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420))
 		ctrl |= IPS_CTRL_YUV | IPS_CTRL_CHD422 | IPS_CTRL_CHD420;
-	else if (st->color_format == DRM_COLOR_FORMAT_YCBCR422)
+	else if (st->color_format == BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR422))
 		ctrl |= IPS_CTRL_YUV | IPS_CTRL_CHD422;
-	else if (st->color_format == DRM_COLOR_FORMAT_YCBCR444)
+	else if (st->color_format == BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR444))
 		ctrl |= IPS_CTRL_YUV;
 
 	/* config dither */
@@ -1265,12 +1265,12 @@ static int dp_improc_init(struct dp_dev *dp, struct block_header *blk,
 
 	improc = to_improc(c);
 	improc->supported_color_depths = BIT(8) | BIT(10);
-	improc->supported_color_formats = DRM_COLOR_FORMAT_RGB444 |
-					  DRM_COLOR_FORMAT_YCBCR444 |
-					  DRM_COLOR_FORMAT_YCBCR422;
+	improc->supported_color_formats = BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444) |
+					  BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR444) |
+					  BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR422);
 	value = linlondp_read32(reg, BLK_INFO);
 	if (value & IPS_INFO_CHD420)
-		improc->supported_color_formats |= DRM_COLOR_FORMAT_YCBCR420;
+		improc->supported_color_formats |= BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420);
 
 	improc->supports_csc = true;
 	improc->supports_gamma = true;
diff --git a/drivers/gpu/drm/cix/linlon-dp/linlondp_cluster.c b/drivers/gpu/drm/cix/linlon-dp/linlondp_cluster.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/linlon-dp/linlondp_cluster.c
+++ b/drivers/gpu/drm/cix/linlon-dp/linlondp_cluster.c
@@ -499,10 +499,9 @@ static int linlondp_cluster_probe(struct platform_device *pdev)
 	return -ENODEV;
 }
 
-static int linlondp_cluster_remove(struct platform_device *pdev)
+static void linlondp_cluster_remove(struct platform_device *pdev)
 {
 	component_master_del(&pdev->dev, &linlondp_cluster_master_ops);
-	return 0;
 }
 
 /*
diff --git a/drivers/gpu/drm/cix/linlon-dp/linlondp_crtc.c b/drivers/gpu/drm/cix/linlon-dp/linlondp_crtc.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/linlon-dp/linlondp_crtc.c
+++ b/drivers/gpu/drm/cix/linlon-dp/linlondp_crtc.c
@@ -46,7 +46,7 @@ void linlondp_crtc_get_color_config(struct drm_crtc_state *crtc_st,
 
 	/* connector doesn't config any color_format, use RGB444 as default */
 	if (!conn_color_formats)
-		conn_color_formats = DRM_COLOR_FORMAT_RGB444;
+		conn_color_formats = BIT(DRM_OUTPUT_COLOR_FORMAT_RGB444);
 
 	*color_depths = GENMASK(min_bpc, 0);
 	*color_formats = conn_color_formats;
diff --git a/drivers/gpu/drm/cix/linlon-dp/linlondp_drv.c b/drivers/gpu/drm/cix/linlon-dp/linlondp_drv.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/linlon-dp/linlondp_drv.c
+++ b/drivers/gpu/drm/cix/linlon-dp/linlondp_drv.c
@@ -277,7 +277,7 @@ static int linlondp_dpu_slave_probe(struct platform_device *pdev)
 	return 0;
 }
 
-static int linlondp_dpu_slave_remove(struct platform_device *pdev)
+static void linlondp_dpu_slave_remove(struct platform_device *pdev)
 {
 	struct linlondp_dev *mdev = platform_get_drvdata(pdev);
 
@@ -286,7 +286,6 @@ static int linlondp_dpu_slave_remove(struct platform_device *pdev)
 	if (mdev)
 		linlondp_dev_destroy(mdev);
 	platform_set_drvdata(pdev, NULL);
-	return 0;
 }
 
 static int linlondp_platform_probe(struct platform_device *pdev)
@@ -337,13 +336,11 @@ static int linlondp_platform_probe(struct platform_device *pdev)
 					       match);
 }
 
-static int linlondp_platform_remove(struct platform_device *pdev)
-{
-	if (device_property_read_bool(&pdev->dev, "cix,linlon-dpu-slave") ||
+static intstatic void linlondp_platform_remove(struct platform_device *pdev)
+{ce_property_read_bool(&pdev->dev, "cix,linlon-dpu-slave") ||
 	    linlondp_acpi_is_cluster_dpu_slave(&pdev->dev))
 		return linlondp_dpu_slave_remove(pdev);
 	component_master_del(&pdev->dev, &linlondp_master_ops);
-	return 0;
 }
 
 static const struct of_device_id linlondp_of_match[] = {
diff --git a/drivers/gpu/drm/cix/linlon-dp/linlondp_pipeline_state.c b/drivers/gpu/drm/cix/linlon-dp/linlondp_pipeline_state.c
index 111111111111..222222222222 100644
--- a/drivers/gpu/drm/cix/linlon-dp/linlondp_pipeline_state.c
+++ b/drivers/gpu/drm/cix/linlon-dp/linlondp_pipeline_state.c
@@ -880,7 +880,7 @@ static int linlondp_improc_validate(struct linlondp_improc *improc,
 			st->color_format = kcrtc_st->output_format;
 		}
 
-		if ((st->color_format == DRM_COLOR_FORMAT_YCBCR420) &&
+		if ((st->color_format == BIT(DRM_OUTPUT_COLOR_FORMAT_YCBCR420)) &&
 		    (improc->base.pipeline->pixel_per_cycle != 1)) {
 			DRM_DEBUG_ATOMIC(
 				"420 with non-1ppc out is not supported\n");
diff --git a/drivers/soc/cix/cix_ap2se_ipc.c b/drivers/soc/cix/cix_ap2se_ipc.c
index 111111111111..222222222222 100644
--- a/drivers/soc/cix/cix_ap2se_ipc.c
+++ b/drivers/soc/cix/cix_ap2se_ipc.c
@@ -250,12 +250,11 @@ static int cix_ap2se_ipc_probe(struct platform_device *pdev)
 	mdev->init_status = 1;
 	return ret;
 }
-static int cix_ap2se_ipc_remove(struct platform_device *pdev)
+static void cix_ap2se_ipc_remove(struct platform_device *pdev)
 {
 	struct cix_ap2se_ipc_dev *mdev = platform_get_drvdata(pdev);
 
 	cix_ap2se_ipc_free_mbox(mdev);
-	return 0;
 }
 
 static int cix_ap2se_ipc_suspend(struct device *dev)
diff --git a/drivers/soc/cix/cix_dst/blackbox/exception_trace/rdr_exception_trace.c b/drivers/soc/cix/cix_dst/blackbox/exception_trace/rdr_exception_trace.c
index 111111111111..222222222222 100755
--- a/drivers/soc/cix/cix_dst/blackbox/exception_trace/rdr_exception_trace.c
+++ b/drivers/soc/cix/cix_dst/blackbox/exception_trace/rdr_exception_trace.c
@@ -234,9 +234,8 @@ static int rdr_exceptiontrace_probe(struct platform_device *pdev)
 	return -1;
 }
 
-static int rdr_exceptiontrace_remove(struct platform_device *pdev)
+static void rdr_exceptiontrace_remove(struct platform_device *pdev)
 {
-	return 0;
 }
 
 static struct platform_driver rdr_et_driver = {
diff --git a/drivers/soc/cix/cix_dst/blackbox/platform_ap/rdr_ap_adapter.c b/drivers/soc/cix/cix_dst/blackbox/platform_ap/rdr_ap_adapter.c
index 111111111111..222222222222 100755
--- a/drivers/soc/cix/cix_dst/blackbox/platform_ap/rdr_ap_adapter.c
+++ b/drivers/soc/cix/cix_dst/blackbox/platform_ap/rdr_ap_adapter.c
@@ -450,9 +450,8 @@ static int rdr_hisiap_probe(struct platform_device *pdev)
 	return 0;
 }
 
-static int rdr_hisiap_remove(struct platform_device *pdev)
+static void rdr_hisiap_remove(struct platform_device *pdev)
 {
-	return 0;
 }
 
 static struct platform_driver rdr_ap_driver = {
diff --git a/drivers/soc/cix/cix_dst/blackbox/rdr_core.c b/drivers/soc/cix/cix_dst/blackbox/rdr_core.c
index 111111111111..222222222222 100644
--- a/drivers/soc/cix/cix_dst/blackbox/rdr_core.c
+++ b/drivers/soc/cix/cix_dst/blackbox/rdr_core.c
@@ -479,10 +479,9 @@ static int rdr_probe(struct platform_device *pdev)
 	return -1;
 }
 
-static int rdr_remove(struct platform_device *pdev)
+static void rdr_remove(struct platform_device *pdev)
 {
 	rdr_dump_exit();
-	return 0;
 }
 
 static struct platform_driver rdr_driver = {
diff --git a/drivers/soc/cix/cix_dst/se_crash_monitor/se_crash_drv.c b/drivers/soc/cix/cix_dst/se_crash_monitor/se_crash_drv.c
index 111111111111..222222222222 100644
--- a/drivers/soc/cix/cix_dst/se_crash_monitor/se_crash_drv.c
+++ b/drivers/soc/cix/cix_dst/se_crash_monitor/se_crash_drv.c
@@ -290,14 +290,13 @@ static int se_crash_probe(struct platform_device *pdev)
 	return ret;
 }
 
-static int se_crash_remove(struct platform_device *pdev)
+static void se_crash_remove(struct platform_device *pdev)
 {
 	struct se_crash_dev *sdev = platform_get_drvdata(pdev);
 
 	rdr_se_unregister_exception();
 	rdr_se_unregister_core(sdev);
 
-	return 0;
 }
 
 static struct platform_driver sp_crash_driver = {
diff --git a/sound/soc/cix/card-utils.c b/sound/soc/cix/card-utils.c
index 111111111111..222222222222 100644
--- a/sound/soc/cix/card-utils.c
+++ b/sound/soc/cix/card-utils.c
@@ -24,7 +24,7 @@ static int cix_jack_init(struct snd_soc_pcm_runtime *rtd)
 	struct snd_soc_dai *codec_dai = snd_soc_rtd_to_codec(rtd, 0);
 	struct cix_asoc_card *priv = snd_soc_card_get_drvdata(rtd->card);
 	struct dai_link_info *link_info =
-		(struct dai_link_info *)((priv->link_info + rtd->num));
+		(struct dai_link_info *)((priv->link_info + rtd->id));
 	struct device *dev = rtd->card->dev;
 	int ret, jack[JACK_CNT], cnt, i;
 
@@ -73,18 +73,19 @@ static int cix_dailink_parsing_fmt(struct device_node *np,
 	struct device_node *framemaster = NULL;
 	unsigned int dai_fmt;
 
-	dai_fmt = snd_soc_of_parse_daifmt(np, NULL,
-				&bitclkmaster, &framemaster);
+	dai_fmt = snd_soc_daifmt_parse_format(np, NULL);
+	snd_soc_daifmt_parse_clock_provider_as_phandle(np, NULL, &bitclkmaster,
+						       &framemaster);
 	if (bitclkmaster != framemaster) {
 		pr_info("Must be the same bitclock and frame master\n");
 		return -EINVAL;
 	}
 	if (bitclkmaster) {
-		dai_fmt &= ~SND_SOC_DAIFMT_MASTER_MASK;
+		dai_fmt &= ~SND_SOC_DAIFMT_CLOCK_PROVIDER_MASK;
 		if (codec_np == bitclkmaster)
-			dai_fmt |= SND_SOC_DAIFMT_CBM_CFM;
+			dai_fmt |= SND_SOC_DAIFMT_CBP_CFP;
 		else
-			dai_fmt |= SND_SOC_DAIFMT_CBS_CFS;
+			dai_fmt |= SND_SOC_DAIFMT_CBC_CFC;
 	}
 	of_node_put(bitclkmaster);
 	of_node_put(framemaster);
@@ -118,7 +119,7 @@ static int dai_set_sysclk(struct snd_pcm_substream *substream,
 	struct snd_soc_pcm_runtime *rtd = snd_soc_substream_to_rtd(substream);
 	struct cix_asoc_card *priv = snd_soc_card_get_drvdata(rtd->card);
 	struct dai_link_info *link_info =
-		(struct dai_link_info *)((priv->link_info + rtd->num));
+		(struct dai_link_info *)((priv->link_info + rtd->id));
 	struct snd_soc_dai *codec_dai;
 	unsigned int mclk, mclk_div, mclk_parent_rate, sample_rate;
 	struct clk *mclk_parent;
@@ -218,7 +219,7 @@ static int dai_startup(struct snd_pcm_substream *substream)
 	struct snd_soc_pcm_runtime *rtd = snd_soc_substream_to_rtd(substream);
 	struct cix_asoc_card *priv = snd_soc_card_get_drvdata(rtd->card);
 	struct dai_link_info *link_info =
-		(struct dai_link_info *)((priv->link_info + rtd->num));
+		(struct dai_link_info *)((priv->link_info + rtd->id));
 	int ret;
 
 	if (link_info->mclk_fs) {
@@ -239,7 +240,7 @@ static void dai_shutdown(struct snd_pcm_substream *substream)
 	struct snd_soc_pcm_runtime *rtd = snd_soc_substream_to_rtd(substream);
 	struct cix_asoc_card *priv = snd_soc_card_get_drvdata(rtd->card);
 	struct dai_link_info *link_info =
-		(struct dai_link_info *)((priv->link_info + rtd->num));
+		(struct dai_link_info *)((priv->link_info + rtd->id));
 
 	if (link_info->mclk_fs) {
 		link_info->mclk_enabled = false;
@@ -254,7 +255,7 @@ static int dai_hw_params(struct snd_pcm_substream *substream,
 	struct snd_soc_pcm_runtime *rtd = snd_soc_substream_to_rtd(substream);
 	struct cix_asoc_card *priv = snd_soc_card_get_drvdata(rtd->card);
 	struct dai_link_info *link_info =
-		(struct dai_link_info *)((priv->link_info + rtd->num));
+		(struct dai_link_info *)((priv->link_info + rtd->id));
 
 	if (link_info->mclk_fs)
 		return dai_set_sysclk(substream, params, link_info->mclk_fs);
@@ -274,9 +275,9 @@ static int cix_dailink_init(struct snd_soc_pcm_runtime *rtd)
 	struct device *dev = rtd->card->dev;
 	struct snd_soc_dai *cpu_dai = snd_soc_rtd_to_cpu(rtd, 0);
 	struct dai_link_info *link_info =
-		(struct dai_link_info *)((priv->link_info + rtd->num));
+		(struct dai_link_info *)((priv->link_info + rtd->id));
 	struct snd_soc_dai_link *dai_link =
-		(struct snd_soc_dai_link *)((priv)->card->dai_link + rtd->num);
+		(struct snd_soc_dai_link *)((priv)->card->dai_link + rtd->id);
 	int ret, fmt;
 
 	dev_dbg(dev, "%s, dai_fmt:0x%x\n", __func__, dai_link->dai_fmt);
@@ -347,7 +348,7 @@ static int cix_card_suspend_post(struct snd_soc_card *card)
 	struct dai_link_info *link_info;
 
 	for_each_card_rtds(card, rtd) {
-		link_info = (struct dai_link_info *)((priv->link_info + rtd->num));
+		link_info = (struct dai_link_info *)((priv->link_info + rtd->id));
 
 		if (link_info->mclk_enabled)
 			clk_disable_unprepare(link_info->clk_mclk);
@@ -364,7 +365,7 @@ static int cix_card_resume_pre(struct snd_soc_card *card)
 	int ret;
 
 	for_each_card_rtds(card, rtd) {
-		link_info = (struct dai_link_info *)((priv->link_info + rtd->num));
+		link_info = (struct dai_link_info *)((priv->link_info + rtd->id));
 
 		if (link_info->mclk_enabled) {
 			ret = clk_prepare_enable(link_info->clk_mclk);
@@ -665,8 +666,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
-		.dpcm_capture = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s0_sc_alc5682),
 	},
@@ -678,7 +677,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s3_mc_alc1019),
 	},
@@ -690,7 +688,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s5_dp0),
 	},
@@ -702,7 +699,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s6_dp1),
 	},
@@ -714,7 +710,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s7_dp2),
 	},
@@ -726,7 +721,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s8_dp3),
 	},
@@ -738,7 +732,6 @@ static struct snd_soc_dai_link sky1_dailink[] = {
 				| SND_SOC_DAIFMT_GATED
 				| SND_SOC_DAIFMT_CBC_CFC,
 		.init = &cix_dailink_init,
-		.dpcm_playback = 1,
 		.ops = &cix_dailink_ops,
 		SND_SOC_DAILINK_REG(cix_i2s9_dp4),
 	},
diff --git a/sound/soc/cix/cdns_i2s_mc.c b/sound/soc/cix/cdns_i2s_mc.c
index 111111111111..222222222222 100644
--- a/sound/soc/cix/cdns_i2s_mc.c
+++ b/sound/soc/cix/cdns_i2s_mc.c
@@ -1086,13 +1086,12 @@ static int cdns_i2s_mc_probe(struct platform_device *pdev)
 	return 0;
 }
 
-static int cdns_i2s_mc_remove(struct platform_device *pdev)
+static void cdns_i2s_mc_remove(struct platform_device *pdev)
 {
 	if (!pm_runtime_status_suspended(&pdev->dev))
 		pm_runtime_force_suspend(&pdev->dev);
 	pm_runtime_disable(&pdev->dev);
 
-	return 0;
 }
 
 static int __maybe_unused cdns_i2s_mc_runtime_suspend(struct device *dev)
diff --git a/sound/soc/sof/cix/cix-sky1.c b/sound/soc/sof/cix/cix-sky1.c
index 111111111111..222222222222 100644
--- a/sound/soc/sof/cix/cix-sky1.c
+++ b/sound/soc/sof/cix/cix-sky1.c
@@ -330,7 +330,7 @@ static int sky1_probe(struct snd_sof_dev *sdev)
 	return ret;
 }
 
-static int sky1_remove(struct snd_sof_dev *sdev)
+static void sky1_remove(struct snd_sof_dev *sdev)
 {
 	struct sky1_priv *priv = sdev->pdata->hw_pdata;
 
@@ -340,7 +340,6 @@ static int sky1_remove(struct snd_sof_dev *sdev)
 
 	platform_device_unregister(priv->ipc_dev);
 
-	return 0;
 }
 
 /* on CIX there is 1 to 1 match between type and BAR idx */
@@ -548,16 +547,16 @@ static struct snd_sof_of_mach sof_sky1_machs[] = {
 
 static struct sof_dev_desc sof_of_sky1_desc = {
 	.of_machines	= sof_sky1_machs,
-	.ipc_supported_mask	= BIT(SOF_IPC),
-	.ipc_default		= SOF_IPC,
+	.ipc_supported_mask	= BIT(SOF_IPC_TYPE_3),
+	.ipc_default		= SOF_IPC_TYPE_3,
 	.default_fw_path = {
-		[SOF_IPC] = "cix/sof",
+		[SOF_IPC_TYPE_3] = "cix/sof",
 	},
 	.default_tplg_path = {
-		[SOF_IPC] = "cix/sof-tplg",
+		[SOF_IPC_TYPE_3] = "cix/sof-tplg",
 	},
 	.default_fw_filename = {
-		[SOF_IPC] = "sof-sky1.ri",
+		[SOF_IPC_TYPE_3] = "sof-sky1.ri",
 	},
 	.nocodec_tplg_filename = "sof-sky1-nocodec.tplg",
 	.ops = &sof_sky1_ops,
@@ -580,6 +579,6 @@ static struct platform_driver snd_sof_of_sky1_driver = {
 };
 module_platform_driver(snd_sof_of_sky1_driver);
 
-MODULE_IMPORT_NS(SND_SOC_SOF_XTENSA);
+MODULE_IMPORT_NS("SND_SOC_SOF_XTENSA");
 MODULE_AUTHOR("Joakim Zhang <joakim.zhang@cixtech.com>");
 MODULE_LICENSE("Dual BSD/GPL");
-- 
Armbian


From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Lukasz Sobala <lsobala1@gmail.com>
Date: Thu, 30 Jul 2026 23:37:03 +0200
Subject: riscv: dts: sophgo: mux the Milk-V Duo S eMMC pads from the DT

On a Duo S with eMMC fitted, booting from a card leaves the eMMC invisible:
the controller registers and then

  mmc2: SDHCI controller on 4300000.mmc [4300000.mmc] using ADMA 64-bit
  mmc2: Failed to initialize a non-removable card

The chip is fine and it is not a bus conflict with the card slot - the two are
separate controllers and the installer drives both in one U-Boot session. The
eMMC's seven pads are simply never routed. In the vendor bootloader,
board/cvitek/cv181x/board.c does

  #if defined(CONFIG_NAND_SUPPORT)
          pinmux_config(PINMUX_SPI_NAND);
  #elif defined(CONFIG_SPI_FLASH)
          pinmux_config(PINMUX_SPI_NOR);
  #elif defined(CONFIG_EMMC_SUPPORT)
          pinmux_config(PINMUX_EMMC);
  #endif

and CONFIG_EMMC_SUPPORT comes from STORAGE_TYPE=emmc at bootloader build time.
So the eMMC build routes them and the SD build does not - the same seven pads
carry SPI-NAND and SPI-NOR, which is why they are switched rather than fixed.
Linux then inherits whatever the BootROM left, and our device tree said nothing
about them, so nothing corrected it.

Declaring them here makes it independent of which bootloader ran, exactly as the
card slot already is: those pads happen to sit in their native function after
either boot, which is why the slot works both ways today.

Mux, pulls and power-source. PINMUX_CONFIG() in the vendor bootloader sets the
function select alone, and cvi_emmc_pad_setting() in its eMMC host driver adds
the pulls - down on CLK, up on CMD, DAT0-DAT3 and RSTN - and leaves drive
strength at its reset value. The group declares exactly that, so the pads are
in the state the eMMC is known to work in whichever bootloader ran.

power-source is not optional: cv1800_dt_node_to_map_post() reads it with
of_property_read_u32() and returns the error if it is absent, which fails the
whole pin map - and then the consumer does not probe at all, so 4300000.mmc
disappears from dmesg entirely rather than failing visibly.

The value is 3300. VDDIO_EMMC also holds UART0_TX/RX, the console on the 3.3V
pin header, so that is the rail's voltage. It also has to be the value every
other group in the domain declares: the driver keeps a single power-source per
domain, and cv1800_set_power_cfg() rejects any group that disagrees with the one
already recorded, with nothing logged at any loglevel. 7.2 has no other group
in the domain to disagree with, but 7.3 gives uart0 one at 3300, and there a
1800 here left a Duo S booting from eMMC in the initramfs, waiting for a root
device that never appeared, with every EMMC_* pad UNCLAIMED in debugfs.

One consequence worth stating: this device tree is shared with the Duo S that
has no eMMC fitted, where it will now pin seven pads for a chip that is not
there. That is inert unless those pads are wired to something else on that
variant, which nothing in the vendor sources suggests - they carry SPI-NAND or
SPI-NOR on boards that have such flash, and the Duo S has neither.

Tested on a Duo S with eMMC, booted from a card: mmcblk2 appears with its
partitions, plus the boot0/boot1/rpmb areas.

Signed-off-by: Armbian <info@armbian.com>
---
 arch/riscv/boot/dts/sophgo/sg2000-milkv-duo-s.dts | 48 ++++++++++++++++++++++-
 1 file changed, 47 insertions(+), 1 deletion(-)

diff --git a/arch/riscv/boot/dts/sophgo/sg2000-milkv-duo-s.dts b/arch/riscv/boot/dts/sophgo/sg2000-milkv-duo-s.dts
index 111111111111..222222222222 100644
--- a/arch/riscv/boot/dts/sophgo/sg2000-milkv-duo-s.dts
+++ b/arch/riscv/boot/dts/sophgo/sg2000-milkv-duo-s.dts
@@ -113,6 +113,50 @@ &dmac {
 	status = "okay";
 };
 
+/*
+ * Route the eMMC pads ourselves instead of inheriting them. The vendor U-Boot
+ * only muxes this group when it was built for eMMC storage
+ * (board/cvitek/cv181x/board.c, guarded by CONFIG_EMMC_SUPPORT), and the same
+ * seven pads are shared with SPI-NAND and SPI-NOR. So after booting the SD build
+ * of the bootloader they are left in whatever the BootROM set, and Linux cannot
+ * talk to the eMMC: "mmc2: Failed to initialize a non-removable card".
+ *
+ * Mux, pulls and power-source. The pulls are the ones the eMMC build of the
+ * bootloader writes in cvi_emmc_pad_setting(): down on CLK, up on the other
+ * six. It leaves drive strength at its reset value and so does this group, so
+ * the pads end up in the state the eMMC is known to work in, whichever
+ * bootloader ran. power-source is not optional: cv1800_dt_node_to_map_post()
+ * rejects any group without it, which fails the consumer's probe outright.
+ *
+ * power-source is 3300 because VDDIO_EMMC is the 3.3V rail on this board: the
+ * domain also holds UART0_TX/RX, the console on header pins 8 and 10, along
+ * with AUX0, SD0_CD, SD0_PWR_EN and the JTAG_CPU pads. cv1800_set_power_cfg()
+ * keeps one value per domain - the first group to probe sets it, and any later
+ * group that disagrees fails its pin map and never probes, silently. Mainline
+ * 7.3 adds a uart0 group at 3300, and against it a 1800 here leaves the eMMC
+ * simply gone.
+ */
+&pinctrl {
+	emmc_cfg: emmc-cfg {
+		emmc-clk-pins {
+			pinmux = <PINMUX(PIN_EMMC_CLK, 0)>;
+			bias-pull-down;
+			power-source = <3300>;
+		};
+
+		emmc-bus-pins {
+			pinmux = <PINMUX(PIN_EMMC_CMD, 0)>,
+				 <PINMUX(PIN_EMMC_DAT0, 0)>,
+				 <PINMUX(PIN_EMMC_DAT1, 0)>,
+				 <PINMUX(PIN_EMMC_DAT2, 0)>,
+				 <PINMUX(PIN_EMMC_DAT3, 0)>,
+				 <PINMUX(PIN_EMMC_RSTN, 0)>;
+			bias-pull-up;
+			power-source = <3300>;
+		};
+	};
+};
+
 &emmc {
 	bus-width = <4>;
 	no-1-8-v;
@@ -120,6 +164,8 @@ &emmc {
 	no-sd;
 	no-sdio;
 	non-removable;
+	pinctrl-names = "default";
+	pinctrl-0 = <&emmc_cfg>;
 	status = "okay";
 };
 
@@ -312,4 +358,4 @@ &int_adc {
 
 &remoteproc {
 	status = "okay";
-};
\ No newline at end of file
+};
-- 
Armbian


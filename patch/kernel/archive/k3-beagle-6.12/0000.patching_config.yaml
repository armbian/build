config:

  # Copy BeagleY-AI board supplements into the TI device-tree source directory.
  # The small patch in this series only includes the supplement from the
  # upstream board DTS, keeping the maintained board description intact.
  dts-directories:
    - { source: "dt", target: "arch/arm64/boot/dts/ti" }

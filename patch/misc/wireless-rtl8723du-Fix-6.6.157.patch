diff --git a/drivers/net/wireless/rtl8723du/os_dep/usb_intf.c b/drivers/net/wireless/rtl8723du/os_dep/usb_intf.c
--- a/drivers/net/wireless/rtl8723du/os_dep/usb_intf.c
+++ b/drivers/net/wireless/rtl8723du/os_dep/usb_intf.c
@@ -149,7 +149,10 @@
 	.usbdrv.supports_autosuspend = 1,
 #endif
 
-#if (LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 19))
+/* 6.6.157 backported the removal of struct usbdrv_wrap into the 6.6 LTS
+ * series, so a plain "older than 6.8" test is no longer enough there. */
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 19)) && \
+	(LINUX_VERSION_CODE < KERNEL_VERSION(6, 6, 157) || LINUX_VERSION_CODE >= KERNEL_VERSION(6, 7, 0))
 	.usbdrv.drvwrap.driver.shutdown = rtw_dev_shutdown,
 #else
 	.usbdrv.driver.shutdown = rtw_dev_shutdown,

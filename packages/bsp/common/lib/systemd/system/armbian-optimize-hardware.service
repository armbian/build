# Armbian optimise hardware service
# Apply optimisations
# This service may block the boot process for up to 2 minutes

[Unit]
Description=Armbian optimise hardware service
Before=basic.target
After=armbian-setup-hardware.target
DefaultDependencies=no

[Service]
Type=oneshot
ExecStart=/usr/lib/armbian/armbian-optimize-hardware start
ExecStop=/usr/lib/armbian/armbian-optimize-hardware stop
RemainAfterExit=yes
TimeoutStartSec=2min

[Install]
WantedBy=basic.target

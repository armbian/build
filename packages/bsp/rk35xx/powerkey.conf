# No PMIC: poweroff parks the SoC with the rails up, so suspend is both the cheaper
# state and the faster to resume. Long press reaches poweroff only over BLE - an IR
# remote stops repeating well before systemd's hardcoded 5 s threshold.
[Login]
HandlePowerKey=suspend
HandlePowerKeyLongPress=poweroff

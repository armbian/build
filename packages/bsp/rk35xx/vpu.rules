# The VPU nodes ship root-only 0600, so hardware decode silently falls back to
# software for anyone but root — and dma_heap is where MPP gets its buffers.
KERNEL=="mpp_service|rga|iep", GROUP="video", MODE="0660"
SUBSYSTEM=="dma_heap", GROUP="video", MODE="0660"

#
# SPDX-License-Identifier: GPL-2.0
#
# Copyright (c) 2013-2023 Igor Pecovnik, igor@armbian.com
#
# This file is a part of the Armbian Build Framework
# https://github.com/armbian/build/
#

source "${BASH_SOURCE%/*}/include/k3_common.inc"

declare -g LINUXFAMILY="k3-beagle" # Separate kernel package from the regular `k3` family
declare -g ATFSOURCE="https://github.com/TexasInstruments/arm-trusted-firmware"

case "${BRANCH}" in

	vendor | vendor-rt)

		declare -g KERNELSOURCE="https://github.com/beagleboard/linux" # BeagleBoard kernel
		declare -g KERNEL_MAJOR_MINOR="6.12"
		declare -g KERNELBRANCH="branch:v6.12.49-ti-arm64-r56"
		declare -g BOOTSOURCE="https://github.com/beagleboard/u-boot" # BeagleBoard u-boot
		declare -g BOOTBRANCH="branch:v2025.07-am6232-pocketbeagle2"
		declare -g KERNEL_DESCRIPTION="BeagleBoard.org (vendor) kernel"
		declare -g ATFBRANCH="tag:11.00.09"
		declare -g OPTEE_BRANCH="tag:4.6.0"
		# PSDK Analytics 11.02.01 uses ti-linux-firmware 11.02.15.  The
		# matching J722S SYSFW/DM IPC ABI is required by the EdgeAI 11.02
		declare -g TI_LINUX_FIRMWARE_BRANCH="tag:11.02.15"
		# artifact-uboot.sh does not otherwise hash TI_LINUX_FIRMWARE_BRANCH.
		# Fold it into the package version so an old tiboot3.bin cannot be
		# reused from Armbian's U-Boot artifact cache.
		declare -g UBOOT_HASH_EXTRA="ti-linux-firmware-11.02.15"
		EXTRAWIFI="no"
		;;

esac

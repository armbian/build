# Run the SDDM greeter under Wayland: the DSI panel is portrait-mounted and
# only Wayland compositors honor the DRM panel-orientation property.
[General]
DisplayServer=wayland
GreeterEnvironment=QT_QPA_PLATFORM=wayland

#!/bin/sh
# Re-apply the CP2 no-sleep setting after resume, in case the WCN suspend/resume
# path reset it. try-restart, not restart: the unit ships disabled and this must
# not turn it on for someone who never asked for it.
case "$1" in
	post) systemctl --no-block try-restart rotate-wcn-nosleep.service ;;
esac

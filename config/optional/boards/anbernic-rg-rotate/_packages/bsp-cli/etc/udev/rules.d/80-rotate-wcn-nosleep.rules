# Re-apply the CP2 no-sleep setting whenever wlan0 (re)appears: a WCN
# power cycle (rfkill, driver reload) reboots CP2 and loses "at+debug=1".
SUBSYSTEM=="net", ACTION=="add", KERNEL=="wlan0", RUN+="/bin/systemctl --no-block restart rotate-wcn-nosleep.service"

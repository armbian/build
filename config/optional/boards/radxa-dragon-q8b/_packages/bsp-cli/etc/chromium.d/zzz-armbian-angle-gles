# Chromium on native freedreno via ANGLE-GLES (matches Firefox): clean UI + GPU.
# zink and the V4L2 GL decode path corrupt the 4K render, so video decodes on CPU.
export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --ozone-platform-hint=auto --use-gl=angle --use-angle=gles --ignore-gpu-blocklist"

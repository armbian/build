apiVersion: 1

# # List of data sources to delete from the database.
# deleteDatasources:
# 
#   - name: InfluxDB_v1
#     orgId: 1

datasources:
  - name: InfluxDB_v1
    isDefault: true
    type: influxdb
    url: http://localhost:8086
    access: proxy
    database: ethonrpi
    user: geth
    secureJsonData:
      password: geth
